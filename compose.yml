services:
  photodrop:
    build: .
    ports:
      - "8080:8080"
    environment:
      PHOTODROP_LISTEN_ADDR: ":8080"
      PHOTODROP_DATA_DIR: /data
      PHOTODROP_BASE_URL: "${PHOTODROP_BASE_URL:-}"
    volumes:
      - ./data:/data
    restart: unless-stopped
    stop_grace_period: 15s
